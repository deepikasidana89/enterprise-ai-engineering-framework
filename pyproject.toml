[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "earf"
version = "0.1.0-dev"
description = "Enterprise AI Readiness Framework (Phase 1 skeleton)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["typer>=0.10.0"]

[project.urls]
Homepage = "https://github.com/deepikasidana89/enterprise-ai-engineering-framework"
Repository = "https://github.com/deepikasidana89/enterprise-ai-engineering-framework"
Issues = "https://github.com/deepikasidana89/enterprise-ai-engineering-framework/issues"

[project.scripts]
earf = "earf.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88

[tool.mypy]
python_version = 3.11
ignore_missing_imports = true

[project.optional-dependencies]
dev = [
	"pytest>=7.0",
	"pytest-cov",
	"ruff",
	"mypy",
]
